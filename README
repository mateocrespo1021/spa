## Run the ``npm i`` command to install the dependencies node modules

## Run the ``npm run dev`` command at the beginning and after making some changes in the scss to compile correctly and generate the production build